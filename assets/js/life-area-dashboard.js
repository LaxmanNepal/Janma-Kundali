import { lifeAreaSummary } from '../../src/life-areas.js';
import { calculateCurrentTransits } from './astrology.js';
import { strengthSummary } from '../../src/planetary-strength.js';
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
let last=null,busy=false;
const icon={career:'💼',finance:'💰',marriage:'❤️',health:'🌿',education:'📚',travel:'✈️',business:'🚀'};
async function render(data){if(!data||data===last||busy)return;busy=true;try{const transit=await calculateCurrentTransits(data,{at:new Date()});const strength=strengthSummary(data);const result=lifeAreaSummary(data,transit,strength);const host=document.getElementById('lifeAreaDashboard');if(!host)return;last=data;host.innerHTML=`<div class="panel la-panel"><div class="panel-title"><div><h3>Life Area Intelligence · V5.5</h3><span>D1 · D9 · Dasha · Gochar · Planetary Strength · Yoga</span></div><strong>${result.overall}/100</strong></div><div class="la-overview"><div><small>समग्र जीवन संकेत</small><b>${result.overall}/100</b></div><div><small>बलियो क्षेत्र</small><b>${icon[result.strongest?.key]||'✦'} ${esc(result.strongest?.label||'—')}</b><span>${result.strongest?.score??0}/100</span></div><div><small>ध्यान क्षेत्र</small><b>${icon[result.focus?.key]||'✦'} ${esc(result.focus?.label||'—')}</b><span>${result.focus?.score??0}/100</span></div></div><div class="la-grid">${result.areas.map(a=>`<article class="la-card"><div class="la-head"><div><b>${a.icon} ${esc(a.label)}</b><span>${esc(a.level)}</span></div><strong>${a.score}</strong></div><div class="la-bar"><i style="width:${a.score}%"></i></div><div class="la-meta"><span>जन्म ${a.natalScore}</span><span>दशा ${a.dashaScore}</span><span>गोचर ${a.transitScore}</span><span>ग्रह ${a.planetScore}</span><span>योग +${a.yogaScore}</span></div><p>${esc(a.summary)}</p><details><summary>किन?</summary><p>मुख्य प्रभाव: ${a.influences.length?a.influences.map(x=>`${esc(x.planet)} (${x.house}H)`).join(' · '):'मुख्य hit छैन'}${a.dashaLord?` · ${esc(a.dashaLord)} महादशा`:''}</p></details></article>`).join('')}</div><p class="accuracy-note">V5.5 परम्परागत ज्योतिषीय नियममा आधारित simplified heuristic हो। D1/D9, दशा, गोचर, ग्रह-बल र योगलाई एउटै decision-support score मा संयोजन गरिएको हो; निश्चित भविष्यवाणी वा वैज्ञानिक मापन होइन।</p></div>`}finally{busy=false}}
function init(){const result=document.getElementById('result');if(!result)return;const mount=()=>{let host=document.getElementById('lifeAreaDashboard');if(!host){host=document.createElement('section');host.id='lifeAreaDashboard';host.className='life-area-dashboard';const target=document.getElementById('planetaryStrengthDashboard')||document.getElementById('yogaDashboard')||document.getElementById('fullReport')||document.getElementById('extendedReport');target?.before(host)}if(window.__kundali)void render(window.__kundali)};setTimeout(mount,2400);setInterval(mount,3000)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
