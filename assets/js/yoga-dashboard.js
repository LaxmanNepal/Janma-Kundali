import { detectYogas } from '../../src/yoga.js';
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
let last=null;
function render(data){if(!data||data===last)return;last=data;const host=document.getElementById('yogaDashboard');if(!host)return;const yogas=detectYogas(data);host.innerHTML=`<div class="panel yoga-panel"><div class="panel-title"><div><h3>योग Intelligence · V5.3</h3><span>परम्परागत योग detection</span></div><strong>${yogas.length} योग</strong></div>${yogas.length?`<div class="yoga-grid">${yogas.map(y=>`<article class="yoga-card ${y.importance}"><div><b>${esc(y.name)}</b><span>${y.importance==='high'?'महत्त्वपूर्ण':'सामान्य'}</span></div><p>${esc(y.reason)}</p></article>`).join('')}</div>`:`<div class="yoga-empty">हालको rule-set मा उल्लेखनीय योग match भेटिएन।</div>`}<p class="accuracy-note">योगको नाम र व्याख्या परम्परागत ज्योतिषीय नियममा आधारित heuristic हो; यसलाई निश्चित भविष्यवाणी वा वैज्ञानिक प्रमाण नमान्नुहोस्।</p></div>`}
function init(){const result=document.getElementById('result');if(!result)return;const mount=()=>{let host=document.getElementById('yogaDashboard');if(!host){host=document.createElement('section');host.id='yogaDashboard';host.className='yoga-dashboard';const target=document.getElementById('fullReport')||document.getElementById('extendedReport');target?.before(host)}if(window.__kundali)render(window.__kundali)};setTimeout(mount,2200);setInterval(mount,1500)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
